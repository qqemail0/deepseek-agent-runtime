# Agent Rules

- Prefer reading/searching before editing.
- Keep tool arguments minimal.
- Do not run destructive commands unless explicitly approved.
- Do not load full skill bodies unless a task matches the skill.
- Keep stable prompt sections byte-for-byte consistent across turns.
- Put dynamic task data after the stable prefix.
